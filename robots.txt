# Irene's Webworks — crawl rules
#
# The app pages below are disallowed because they are useless to a searcher and
# harmful to rank for: overview/editor/purchased require a session and render as an
# empty shell to a crawler, and a thin logged-out page competing with the marketing
# pages splits the site's signals for no gain.
#
# Everything else — the marketing pages and, more importantly, the sample galleries —
# is open. The galleries are the strongest organic asset here: twenty complete example
# portfolios covering distinct majors, which is exactly what someone searching
# "biology portfolio website example" is looking for.

User-agent: *
Allow: /

Disallow: /src/overview.html
Disallow: /src/editor.html
Disallow: /src/purchased.html
Disallow: /netlify/
Disallow: /.netlify/

Sitemap: https://resumeto.website/sitemap.xml
